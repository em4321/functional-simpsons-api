// import React from "react";
// const Image = ({ src, alt }) => {
//   //Defensive check
//   return typeof src !== "string" ||
//     typeof alt !== "string" ||
//     src.length === 0 ||
//     alt.length === 0 ? (
//     <p>Image is not available</p>
//   ) : (
//     <img src={src} alt={alt} />
//   );
// };

// export default Image;
